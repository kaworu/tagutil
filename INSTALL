Installation
------------
You need taglib 1.5 (http://developer.kde.org/~wheeler/taglib.html) and
pmk 0.10.4 (http://pmk.sourceforge.net/) to build tagutil.

to build and install tagutil, type:
pmk
make
make install

you can select the PREFIX installation. for example, GNU/Linux distro would
type:

make PREFIX=/usr install
