Installation
------------
You need taglib (http://developer.kde.org/~wheeler/taglib.html) and
pmk (http://pmk.sourceforge.net/) to build tagutil.

to build and install tagutil, type:
pmk
make
make install

you can select the PREFIX installation. for example, GNU/Linux distro would
type:

make PREFIX=/usr install
